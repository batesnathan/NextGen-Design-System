# Semantics

Semantics map design intent to implementation values.

Current baseline semantic theme:

- surface (`default`, `secondary`)
- text (`default`, `secondary`)
- border (`default`)
- interactive (`primary`, `secondary`)

See `src/semantics/theme.ts` for mapping logic and CSS variable export.
